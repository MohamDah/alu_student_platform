import 'package:flutter/material.dart';

class ALUColors {
  static const Color navyBlue = Color.fromRGBO(0, 46, 109, 1); // Deep Navy for background
  static const Color red = Color.fromRGBO(208, 13, 45, 1);   // ALU Yellow for buttons
  static const Color white = Colors.white;
  static const Color grey = Color(0xFFE0E0E0);
  static const Color redRisk = Color(0xFFD32F2F);  // For the "At Risk" warning
}